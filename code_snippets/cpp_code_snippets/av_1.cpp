#include <iostream>
using namespace std;

// Declare that there is a function called average
// (but don't define it here)
double average(double, double);

int main() {
	double v1 = 123.95;
	double v2 = 22;
	cout << "The average is : " << average (v1, v2) << endl;
}
